---
import BaseLayout from '../layouts/BaseLayout.astro';
import Terminal from '../components/Terminal';
---

<BaseLayout>
  <!-- Mount the terminal as a client-side only React island -->
  <Terminal client:only="react" />
</BaseLayout>
